% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adult_support.R
\name{remove_ewma_wt}
\alias{remove_ewma_wt}
\title{Remove EWMA weight outliers (used for both Extreme and Moderate EWMA)}
\usage{
remove_ewma_wt(
  subj_df,
  wtallow_formula = "piecewise",
  exc_label = "Exclude-A-WT-Traj-Ext",
  ewma_window = 15
)
}
\arguments{
\item{subj_df}{Data frame with age_days, meas_m, id columns (pre-sorted)}

\item{wtallow_formula}{Formula for ET cap computation}

\item{exc_label}{Exclusion code label assigned to excluded values.}
}
\value{
Named character vector: id -> exc_label for excluded values
}
\description{
Iteratively excludes values whose EWMA deviation exceeds an interval-specific
threshold (ET cap). Each round removes the single worst outlier (largest
|dewma|, with age then internal_id as tiebreakers). Iterates until no candidates
remain or <3 values.
}
\details{
Threshold: ET cap derived from wtallow formula and upper weight (UW).
Per-observation threshold based on min neighbor gap and max(wt, ewma).

90\% rule: directional dewma (before/after) must exceed 90\% of threshold.
Missing neighbors (edge values) are treated as Inf (confirming exclusion).
}
\keyword{internal}
