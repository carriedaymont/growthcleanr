% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adult_support.R
\name{propagate_to_rv}
\alias{propagate_to_rv}
\title{Propagate firstRV exclusions forward to RV copies and extraneous values
(linked mode). For each excluded value, finds all other values with the
same measurement that are still "Include" — including extraneous values
whose is_rv flag may have been cleared by redo_identify_rv(). Marks them
with "\if{html}{\out{<code>}}-RV-propagated". This is forward-only propagation (firstRV →
later RVs + extraneous), unlike Step 4W's bidirectional scale-max propagation.}
\usage{
propagate_to_rv(exc_codes, w_subj_df, w_subj_keep)
}
\arguments{
\item{exc_codes}{Named character vector: id -> exclusion code (from firstRV pass)}

\item{w_subj_df}{Current working weight data (must have meas_m, id columns)}

\item{w_subj_keep}{Named vector of all exclusion codes for this subject's weights}
}
\value{
List with updated w_subj_keep and propagated_ids
}
\description{
Propagate firstRV exclusions forward to RV copies and extraneous values
(linked mode). For each excluded value, finds all other values with the
same measurement that are still "Include" — including extraneous values
whose is_rv flag may have been cleared by redo_identify_rv(). Marks them
with "\if{html}{\out{<code>}}-RV-propagated". This is forward-only propagation (firstRV →
later RVs + extraneous), unlike Step 4W's bidirectional scale-max propagation.
}
\keyword{internal}
