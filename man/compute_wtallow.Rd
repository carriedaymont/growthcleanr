% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adult_support.R
\name{compute_wtallow}
\alias{compute_wtallow}
\title{Compute weight allowance from interval in months (vectorized)}
\usage{
compute_wtallow(months, formula = "piecewise", uw = NULL)
}
\arguments{
\item{months}{Numeric vector of interval lengths in months}

\item{formula}{Character: formula name or path to custom CSV}

\item{uw}{Optional numeric vector of upper weights (higher of two adjacent
weights, or max(wt, ewma)). NULL = use base (UW=120) formula.}
}
\value{
Numeric vector of weight allowances
}
\description{
Built-in formulas: "piecewise" (PW-H, default), "piecewise-lower" (PW-L),
"allofus15". Custom: pass a file path to a CSV with columns 'months' and
'wtallow'. Values are linearly interpolated; months beyond the table are
clamped to the nearest row.
}
\details{
UW (upper weight) scaling:
\itemize{
\item PW-H: scaled up for UW > 120 (slope 0.25/kg, UW capped at 180) and
down for UW < 120, with 2/3 ceiling.
\item PW-L: NOT scaled up for UW > 120. Scaled down for UW < 120, with 2/3
ceiling.
\item allofus15: not adjusted by UW, except its 12m cap is limited to never
exceed the effective PW-L at 12m for that UW.
\item Custom CSV: no UW adjustment.
}
}
\keyword{internal}
